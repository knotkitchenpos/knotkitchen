VITE_BACKEND_URL=
VITE_RAZORPAY_KEY_ID=

# For an APK/tablet connecting to a deployed backend, use its HTTPS public URL.
# Do not use localhost unless the backend is running on the same device.
# VITE_BACKEND_URL=https://pos-api.example.com